import styled from "styled-components";

export const StyledWishlistContent = styled.div`
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));;
    grid-gap: 20px 50px;
`